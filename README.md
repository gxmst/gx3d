# GxEngine

GxEngine 是一个学习型 Rust 3D 引擎 / FPS sandbox。当前目标不是做成通用商用引擎，而是用尽量清楚的模块边界串起一个可运行、可调试、可继续扩展的 3D 游戏骨架。

## 当前状态

- Rust 2021 + winit + wgpu 窗口和渲染主循环
- hecs ECS 世界、资源容器和分阶段调度器
- Rapier3D 物理世界、刚体/碰撞体、射线检测、基础同步
- PBR 前向渲染、方向光阴影、程序化 IBL、SSAO、Bloom、ACES 后处理
- 程序化几何体和基础 glTF/GLB 静态网格加载
- FPS 摄像机、玩家移动、射击射线、后坐力、简单敌人 AI
- 物理 sandbox：质量差异、摩擦/弹性材质、坡道、冰面、滑块、可击倒靶、生成/拾取/投掷动态物体

## 运行

```powershell
cargo run
```

首次构建会比较慢。窗口标题会显示 FPS、弹药、灯光数量和相机位置。
启动时默认创建 1920×1080 的窗口化画面并居中，鼠标会进入 FPS 锁定/隐藏状态。主循环最高 144 FPS。

## 控制

| 操作 | 输入 |
| --- | --- |
| 移动 | WASD |
| 奔跑 | Left Shift |
| 跳跃 | Space |
| 视角 | 鼠标移动 |
| 射击 | 鼠标左键 |
| 生成物理箱子 | G |
| 生成弹力球 | B |
| 生成重箱子 | H |
| 拾取/放下物体 | E 或鼠标中键 |
| 调节持物距离 | 鼠标滚轮 |
| 投掷持物/推准星物体 | T |
| 冻结/解冻准星物体 | X |
| 按住显示控制提示 | F |
| 打开/关闭设置菜单 | Esc |

## 物理测试区

启动场景里已经摆了一组可测试物件：

- 左侧坡道和一组绿色弹力球，用来观察弹性碰撞和滚落。
- 青色冰面和两个圆柱滑块，用来对比低摩擦与普通摩擦。
- 黄色轻箱和深色重箱，用来对比质量、拾取和投掷手感。
- 红色薄靶，用来测试射击、推撞和连锁倒塌。

`E` 或中键对准动态物体可抓取，滚轮拉近/推远，`T` 可以把持物抛出去。
鼠标左键射击会在命中位置留下短暂弹孔和闪光，命中动态物体时会附带小幅击退。
设置菜单打开时会释放鼠标，右侧显示键位提示，左侧可切换窗口分辨率和简体中文语言项。

## 架构笔记

- `src/core`：ECS 包装、资源容器、时间和调度。
- `src/renderer`：wgpu 初始化、渲染管线、bind group、后处理、SSAO、IBL。
- `src/physics`：Rapier3D 封装、碰撞形状、射线和物理 sandbox。
- `src/asset`：网格、材质、纹理、程序化资源和 glTF 加载。
- `src/game`：当前 FPS sandbox 的玩家、武器、敌人和系统注册。
- `assets/shaders`：WGSL shader。主 PBR pass 输出线性 HDR，tone mapping 在后处理完成。

## 下一步路线

1. 稳住输入、物理同步、渲染后处理这些底座。
2. 把测试场景做成明确的物理 playground：更多可拾取物、斜坡、弹射、不同质量。
3. 把 glTF loader 从“能读静态 mesh”推进到“尊重 node transform 和 primitive material”。
4. 再考虑动画、骨骼、IK、编辑器或资源 cooking。

设计野心放在 `docs/DESIGN.md`，实际进度以 README 和测试为准。
